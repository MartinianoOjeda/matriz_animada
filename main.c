#include <stdio.h>
#define MAX 10

typedef char matriz[MAX][MAX];


matriz * crearMatriz();


int main() {

    return 0;
}

matriz * crearMatriz(matriz nueva_matriz) {

    int i, j;

    for(i = 0; i < MAX; i++) {
        for(j = 0; j < MAX; j++) {

        }
    }
    return * nueva_matriz;
}